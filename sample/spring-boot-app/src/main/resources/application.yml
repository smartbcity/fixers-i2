spring:
  main:
    web-application-type: reactive

keycloak:
  auth-server-url: https://auth.smart-b.io/auth
  realm: test
  clients:
    admin:
      realm: master
      clientId: admin-cli
      username: smartb
      password: conorS

i2:
  jwt-issuer-uri: ${keycloak.auth-server-url}/realms/${keycloak.realm}
  #jwk-set-uri: ${keycloak.auth-server-url}/realms/${keycloak.realm}/protocol/openid-connect/certs
  #filter:
    #organizationId: 919036e9-886a-4798-a3d8-d55b3a651540
  organization:
    insee:
      token: c6ef3de5-3ef1-330a-8917-21f6750dcb09
      sirene-api: https://api.insee.fr/entreprises/sirene/V3
  auth-client: admin

server:
  port : 9090
